package com.example.ecolush.ui.theme

import androidx.compose.ui.graphics.Color

// EcoLush Premium Theme (Inspiré du design Dribbble)
val PrimaryBlue = Color(0xFF1E3A8A) // Bleu profond pour le header
val AccentGreen = Color(0xFF22C55E) // Vert vif pour les badges de prix
val BgLight = Color(0xFFF8FAFC)    // Gris très clair pour le fond
val TextDark = Color(0xFF0F172A)    // Presque noir pour le texte
val TextGray = Color(0xFF64748B)    // Gris pour les sous-titres

// Couleurs Material 3 adaptées
val Primary = PrimaryBlue
val Secondary = AccentGreen
val Tertiary = Color(0xFFFACC15) // Jaune pour les alertes/promos

val BackgroundLight = BgLight
val SurfaceLight = Color.White

val PrimaryDark = Color(0xFF93C5FD)
val BackgroundDark = Color(0xFF0F172A)
val SurfaceDark = Color(0xFF1E293B)
